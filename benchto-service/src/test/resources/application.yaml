spring:
  datasource:
    url: jdbc:tc:postgresql:16:///databasename
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    database: postgresql
    hibernate.ddl-auto: validate

flyway:
  enabled: true
