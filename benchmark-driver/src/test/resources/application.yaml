spring:
  datasource:
    url: jdbc:hsqldb:mem:.
    username: sa
    password:
    driver-class-name: org.hsqldb.jdbcDriver

benchmark-service:
  url: http://localhost:8080

environment:
  name: TEST_ENV

presto:
  url: http://presto-test-master:8090