datasource: presto
query-names: presto/linear-scan-insert-single/selectivity-${selectivity}.sql
database: hive
before-benchmark: presto/linear-scan-insert-single/create-insert-table.sql
after-benchmark: presto/linear-scan-insert-single/drop-insert-table.sql
table: tmp_insert_selectivity_${selectivity}_concurrency_${concurrency_level}
runs: ${concurrency_level}
concurrency: ${concurrency_level}
prewarm-runs: 3
before-benchmark: drop-caches
quarantine: true
variables:
  1:
    selectivity: 10, 100
    concurrency_level: 10, 20, 30
    schema: tpch_100gb_orc
    quarantine: true
  2:
    selectivity: 10, 100
    concurrency_level: 10, 20, 30
    schema: tpch_100gb_text